//
//  PresenterTests.swift
//  unit_testTests
//
//  Created by Maxence HO on 23/10/2020.
//

import XCTest

class PresenterTests: XCTestCase {
    func test_didLoad_success_modeA() {
        // Tester que quand l'appel réussi et si 'mode' == 'mode A' alors 'datasource' == "A_test"
    }
    
    func test_didLoad_success_modeB() {
        // Tester que quand l'appel réussi et si 'mode' == 'mode B' alors 'datasource' == "B_test"
    }
    
    func test_didLoad_error() {
        // Tester que quand l'appel fail 'new_datasource' == 'old_datasource'
    }
}


// Est-ce que cette stratégie de test est unitaire?
// Comment écrire ces tests ?
